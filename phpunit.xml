<?xml version="1.0" encoding="UTF-8"?>
<!--
    IMPORTANTE: Os testes devem ser rodados contra PHP 8.3, que é a versão
    usada tanto no Valet (dev) quanto em produção (AWS EC2).
    Use PHP 8.3 no PATH: php vendor/bin/pest
    O config.platform.php no composer.json garante que as dependências sejam
    resolvidas para PHP 8.3, mas a execução dos testes deve usar o binário correto.

    Suíte "MySQL": integração real (FULLTEXT, fixtures legados). Ver docker-compose.yml
    e ARQUIVOS_MD/METERED_WALL_PLAN.md. Comando recomendado: composer test (sobe MySQL em Docker).
-->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
        <testsuite name="Arch">
            <directory>tests/Arch</directory>
        </testsuite>
        <testsuite name="MySQL">
            <directory>tests/MySQL</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <server name="APP_ENV" value="testing"/>
        <server name="BCRYPT_ROUNDS" value="4"/>
        <server name="CACHE_DRIVER" value="array"/>
        <server name="DB_CONNECTION" value="sqlite"/>
        <server name="DB_DATABASE" value=":memory:"/>
        <server name="MAIL_MAILER" value="array"/>
        <server name="QUEUE_CONNECTION" value="sync"/>
        <server name="SESSION_DRIVER" value="array"/>
        <server name="TELESCOPE_ENABLED" value="false"/>
    </php>
</phpunit>
